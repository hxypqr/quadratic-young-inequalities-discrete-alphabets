# Manuscript

`main.tex` is the canonical editable source for the paper. The PDF in this
directory is the latest checked build intended for convenient reading.

LaTeX auxiliary files are intentionally excluded from version control. When the
source changes, rebuild the PDF twice so that the table of contents, references,
and internal links are current.
